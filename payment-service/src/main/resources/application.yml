server:
  port: 7085

spring:
  application:
    name: payment-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    export:
      prometheus:
        enabled: true

otel:
  service:
    name: payment-service
  exporter:
    otlp:
      endpoint: http://otel-collector:4318
  instrumentation:
    logback-appender:
      enabled: true
    spring-webmvc:
      enabled: true
