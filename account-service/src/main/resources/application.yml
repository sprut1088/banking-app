server:
  port: 7082

spring:
  application:
    name: account-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    export:
      prometheus:
        enabled: true

otel:
  service:
    name: account-service
  exporter:
    otlp:
      endpoint: http://otel-collector:4318
  instrumentation:
    logback-appender:
      enabled: true
    spring-webmvc:
      enabled: true
